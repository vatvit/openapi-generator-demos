<?php
/**
 * Tic Tac Toe
 * API Routes - Auto-generated by OpenAPI Generator
 *
 * @version 1.0.0
 * @link https://openapi-generator.tech
 */

/**
 * NOTE: This file is auto-generated. Include it in your Laravel application's routes file.
 *
 * Example integration in routes/api.php:
 *
 *   Route::group(['prefix' => 'v1', 'middleware' => ['api']], function ($router) {
 *       require base_path('vendor/vatvit/vatvit/tictactoe-lumen-laravel/routes.php');
 *   });
 *
 * Or for local development:
 *   Route::group(['prefix' => 'v1', 'middleware' => ['api']], function ($router) {
 *       require base_path('generated/php-lumen/TicTacToeApiV2LumenLaravel/lib/routes.php');
 *   });
 */

/**
 * post /games
 * Operation: createGame
 * Summary: Create a new game
 * Notes: Creates a new TicTacToe game with specified configuration.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->post('/games', [TicTacToeApiV2\Server\Http\Controllers\CreateGameApiController::class, 'createGame'])
    ->name('api.createGame');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.createGame')) {
    $route->middleware('api.middlewareGroup.createGame');
}

/**
 * delete /games/{gameId}
 * Operation: deleteGame
 * Summary: Delete a game
 * Notes: Deletes a game. Only allowed for game creators or admins.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->delete('/games/{gameId}', [TicTacToeApiV2\Server\Http\Controllers\DeleteGameApiController::class, 'deleteGame'])
    ->name('api.deleteGame');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.deleteGame')) {
    $route->middleware('api.middlewareGroup.deleteGame');
}

/**
 * get /games/{gameId}/board
 * Operation: getBoard
 * Summary: Get the game board
 * Notes: Retrieves the current state of the board and the winner.
 * Security:
 *   - defaultApiKey
 *   - app2AppOauth (scopes: board:read)
 */
$route = $router->get('/games/{gameId}/board', [TicTacToeApiV2\Server\Http\Controllers\GetBoardApiController::class, 'getBoard'])
    ->name('api.getBoard');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.defaultApiKey')) {
    $route->middleware('api.security.defaultApiKey');
}
if ($router->hasMiddlewareGroup('api.security.app2AppOauth')) {
    $route->middleware('api.security.app2AppOauth');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getBoard')) {
    $route->middleware('api.middlewareGroup.getBoard');
}

/**
 * get /games/{gameId}
 * Operation: getGame
 * Summary: Get game details
 * Notes: Retrieves detailed information about a specific game.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->get('/games/{gameId}', [TicTacToeApiV2\Server\Http\Controllers\GetGameApiController::class, 'getGame'])
    ->name('api.getGame');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getGame')) {
    $route->middleware('api.middlewareGroup.getGame');
}

/**
 * get /leaderboard
 * Operation: getLeaderboard
 * Summary: Get leaderboard
 * Notes: Retrieves the global leaderboard with top players.
 */
$route = $router->get('/leaderboard', [TicTacToeApiV2\Server\Http\Controllers\GetLeaderboardApiController::class, 'getLeaderboard'])
    ->name('api.getLeaderboard');


// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getLeaderboard')) {
    $route->middleware('api.middlewareGroup.getLeaderboard');
}

/**
 * get /games/{gameId}/moves
 * Operation: getMoves
 * Summary: Get move history
 * Notes: Retrieves the complete move history for a game.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->get('/games/{gameId}/moves', [TicTacToeApiV2\Server\Http\Controllers\GetMovesApiController::class, 'getMoves'])
    ->name('api.getMoves');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getMoves')) {
    $route->middleware('api.middlewareGroup.getMoves');
}

/**
 * get /players/{playerId}/stats
 * Operation: getPlayerStats
 * Summary: Get player statistics
 * Notes: Retrieves comprehensive statistics for a player.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->get('/players/{playerId}/stats', [TicTacToeApiV2\Server\Http\Controllers\GetPlayerStatsApiController::class, 'getPlayerStats'])
    ->name('api.getPlayerStats');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getPlayerStats')) {
    $route->middleware('api.middlewareGroup.getPlayerStats');
}

/**
 * get /games/{gameId}/board/{row}/{column}
 * Operation: getSquare
 * Summary: Get a single board square
 * Notes: Retrieves the requested square.
 * Security:
 *   - bearerHttpAuthentication
 *   - user2AppOauth (scopes: board:read)
 */
$route = $router->get('/games/{gameId}/board/{row}/{column}', [TicTacToeApiV2\Server\Http\Controllers\GetSquareApiController::class, 'getSquare'])
    ->name('api.getSquare');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}
if ($router->hasMiddlewareGroup('api.security.user2AppOauth')) {
    $route->middleware('api.security.user2AppOauth');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.getSquare')) {
    $route->middleware('api.middlewareGroup.getSquare');
}

/**
 * get /games
 * Operation: listGames
 * Summary: List all games
 * Notes: Retrieves a paginated list of games with optional filtering.
 * Security:
 *   - bearerHttpAuthentication
 */
$route = $router->get('/games', [TicTacToeApiV2\Server\Http\Controllers\ListGamesApiController::class, 'listGames'])
    ->name('api.listGames');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.listGames')) {
    $route->middleware('api.middlewareGroup.listGames');
}

/**
 * put /games/{gameId}/board/{row}/{column}
 * Operation: putSquare
 * Summary: Set a single board square
 * Notes: Places a mark on the board and retrieves the whole board and the winner (if any).
 * Security:
 *   - bearerHttpAuthentication
 *   - user2AppOauth (scopes: board:write)
 */
$route = $router->put('/games/{gameId}/board/{row}/{column}', [TicTacToeApiV2\Server\Http\Controllers\PutSquareApiController::class, 'putSquare'])
    ->name('api.putSquare');

// Security middleware auto-attachment (based on OpenAPI security requirements)
if ($router->hasMiddlewareGroup('api.security.bearerHttpAuthentication')) {
    $route->middleware('api.security.bearerHttpAuthentication');
}
if ($router->hasMiddlewareGroup('api.security.user2AppOauth')) {
    $route->middleware('api.security.user2AppOauth');
}

// Custom operation-specific middleware (optional)
if ($router->hasMiddlewareGroup('api.middlewareGroup.putSquare')) {
    $route->middleware('api.middlewareGroup.putSquare');
}

